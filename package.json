{
  "name": "vaken",
  "version": "1.0.0",
  "description": "Next-gen hacker registration system",
  "main": "index.js",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpileOnly src/server.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "format": "prettier --parser typescript --single-quote --trailing-comma=es5 --write \"./**/*.ts\"",
    "lint": "tslint -c tslint.json -p tsconfig.json",
    "lint-fix": "tslint -c tslint.json -p tsconfig.json --fix",
    "start": "ts-node src/server.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "husky": {
    "hooks": {
      "pre-commit": "npm run typecheck && npm run lint && lint-staged"
    }
  },
  "lint-staged": {
    "src/**/*": [
      "npm run format",
      "git add ."
    ]
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/VandyHacks/vaken.git"
  },
  "keywords": [
    "hackathon",
    "registration"
  ],
  "author": "VandyHacks",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/VandyHacks/vaken/issues"
  },
  "homepage": "https://github.com/VandyHacks/vaken#readme",
  "dependencies": {
    "bcrypt": "^3.0.2",
    "koa": "^2.6.2",
    "koa-helmet": "^4.0.0",
    "koa-joi-router": "^5.1.0",
    "koa-jwt": "^3.5.1",
    "mongoose": "^5.3.15",
    "reflect-metadata": "^0.1.12",
    "typegoose": "^5.4.1"
  },
  "devDependencies": {
    "@koa/cors": "^2.2.2",
    "@types/bcrypt": "^3.0.0",
    "@types/koa": "^2.0.47",
    "@types/koa-helmet": "^3.1.2",
    "@types/koa-joi-router": "^5.1.0",
    "@types/koa__cors": "^2.2.3",
    "@types/mongoose": "^5.3.4",
    "husky": "^1.2.0",
    "lint-staged": "^8.1.0",
    "prettier": "^1.15.3",
    "ts-node": "^7.0.1",
    "ts-node-dev": "^1.0.0-pre.32",
    "tslint": "^5.11.0",
    "tslint-config-airbnb": "^5.11.1",
    "tslint-config-prettier": "^1.17.0",
    "typescript": "^3.2.2"
  }
}
